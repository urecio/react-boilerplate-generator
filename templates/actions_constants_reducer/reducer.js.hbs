/*
 *
 * {{ properCase name }} reducer
 *
 */

import { fromJS } from 'immutable';
import * as constants from './constants';

const initialState = fromJS({
  {{#asyncRequestsResourceNameForState}}
  {{ name }}: {},
  {{/asyncRequestsResourceNameForState}}
});

function {{ camelCase name }}Reducer(state = initialState, action) {
  switch (action.type) {
    {{#if wantAsyncRequests}}
      {{#asyncRequests}}
    case constants.{{ upperCase name }}_RESPONSE:
      return state.set('{{ resource }}', action.payload);
      {{/asyncRequests}}
    {{else}}
    case DEFAULT_ACTION:
      return state;
    {{/if}}
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;
